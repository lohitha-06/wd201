{
  "name": "todo-cli",
  "version": "1.0.0",
  "main": "todo.js",
  "scripts": {
    "test": "jest",
    "format": "prettier --write ."
  },
  "devDependencies": {
    "husky": "^8.0.0",
    "jest": "^29.0.0",
    "prettier": "^2.8.0"
  },
  "husky": {
    "hooks": {
      "pre-commit": "npm run format && npm test"
    }
  }
}
